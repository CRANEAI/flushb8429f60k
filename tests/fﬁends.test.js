/* 
 * Contact: team@fusioncode.ai
 *
 *
 * Project  - fﬁends UNIT TESTS
 * Application Client
 * flush
*/

/* the fﬁends view has a profile icon located at the mid-top left of the view which on click will trigger the profile() action */
it("the fﬁends view has a profile icon located at the mid-top left of the view which on click will trigger the profile() action", () => {
  /* ToDo developer needs to complete unit test 
               the fﬁends view has a profile icon located at the mid-top left of the view which on click will trigger the profile() action
               uncomment below and update the code to test
                //instance.action(function(error) {
                //  if (error) throw error;
                //expect().to.be();
                //});
                done();
               */
})
